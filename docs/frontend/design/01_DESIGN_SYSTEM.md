# 디자인 시스템

> **버전**: 1.0
> **최종 업데이트**: 2026-02-13
> **상태**: Phase 1 설계 중

---

## 1. 색상 팔레트

| 토큰 | 값 | 용도 |
|:---|:---|:---|
| `--primary` | `#2563eb` | 주요 액션, 브랜드 |
| `--primary-dark` | `#1d4ed8` | 호버 상태 |
| `--secondary` | `#64748b` | 보조 텍스트 |
| `--background` | `#ffffff` | 배경 |
| `--foreground` | `#0f172a` | 본문 텍스트 |
| `--muted` | `#f1f5f9` | 비활성 배경 |
| `--border` | `#e2e8f0` | 테두리 |
| `--success` | `#16a34a` | 성공 상태 |
| `--warning` | `#f59e0b` | 경고 상태 |
| `--danger` | `#dc2626` | 에러/삭제 |

---

## 2. 공유 UI 컴포넌트 (@bridgeai/ui-components)

| 컴포넌트 | 용도 | Props |
|:---|:---|:---|
| `Button` | 버튼 | variant, size |
| `Card` | 카드 컨테이너 | title, className |
| `Input` | 입력 필드 | label, error |
| `Badge` | 상태 뱃지 | variant |
| `Modal` | 모달 다이얼로그 | isOpen, onClose, title |

---

## 3. 레이아웃 규칙

### admin-web 레이아웃

```
┌──────────────────────────────────────────┐
│  사이드바 (240px)  │  메인 콘텐츠         │
│  ┌──────────────┐  │  ┌────────────────┐ │
│  │ BridgeAI     │  │  │ 페이지 타이틀    │ │
│  │ Admin        │  │  │                │ │
│  │              │  │  │ 콘텐츠 영역     │ │
│  │ • 대시보드    │  │  │                │ │
│  │ • 강사 관리   │  │  │                │ │
│  │ • 수요처 관리  │  │  │                │ │
│  │ • 매칭/파견   │  │  │                │ │
│  │ • 계약 관리   │  │  └────────────────┘ │
│  │ • 정산 관리   │  │                     │
│  │ • 평가 관리   │  │                     │
│  │ • 설정       │  │                     │
│  └──────────────┘  │                     │
└──────────────────────────────────────────┘
```

---

## 변경 이력

| 버전 | 날짜 | 변경 내용 |
|:---|:---|:---|
| 1.0 | 2026-02-13 | 초기 문서 작성 |
